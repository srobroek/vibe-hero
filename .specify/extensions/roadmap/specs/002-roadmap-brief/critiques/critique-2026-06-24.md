# Critique Report: Roadmap Brief — Pre-Implementation Review

**Date**: 2026-06-24
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Verdict**: ✅ PROCEED

---

## Executive Summary

Spec 002 is tightly scoped and low-risk: a read-only, judgment-only review command that
reuses already-verified infrastructure (load-config + core check-prerequisites), adds one
shared report template, and ships no new executable code. The constitution check passes on
all seven principles, and the command was already dogfooded on spec 001 (it correctly
surfaced real drift). No must-address items. A few recommendations sharpen robustness and
honesty of the success criteria, but none block `/speckit.tasks`.

---

## Product Lens Findings 🎯

### Problem Validation
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P1 | 💡 | The value ("ground the build in earlier intent") is real but the spec doesn't state what failure looks like *without* the brief, making SC hard to attribute. | Already partly covered by US1's rationale; add a one-line note that the brief's value is measured by whether implementers catch divergence they'd otherwise miss. |

### User Value Assessment
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P2 | 💡 | The brief outputs a *report file*. For a quick pre-implementation check, a file may be heavier than an inline summary. | Acceptable — the file mirrors critique/verify conventions and creates an audit trail. Consider noting that the verdict + top findings are also echoed to the user inline (not only written to the file). |

### Edge Cases & UX
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P3 | 🤔 | When the active spec has no matching ledger entry, the brief stops and suggests adding it. Is "stop" the right UX, or should it offer a minimal briefing from the spec alone? | Keep "stop + suggest" (don't fabricate roadmap context); the spec text is already available to the implementer. Confirm this is the intended behavior. |

### Success Measurement
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P4 | 💡 | SC-003 ("divergence appears as a drift finding") depends on judgment and can't be mechanically asserted. | Note in the spec that drift-detection quality is validated by dogfood/review, not an automated test — consistent with the determinism split. |

## Engineering Lens Findings 🔬

### Architecture Soundness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E1 | 💡 | The shared `review-report-template.md` couples brief/debrief/sync to one skeleton. A later change to suit one could ripple to the others. | Acceptable (DRY + conformance). Keep the template general (kind-agnostic sections) so each command fills only what applies — already the design. |

### Failure Mode Analysis
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E2 | 💡 | Entry matching (spec-dir → title → number) could mis-match if two entries have similar titles. | Add a tie-break rule: if title matching is ambiguous, report the candidates and ask rather than guessing (mirrors the no-fabrication stance). |

### Testing Strategy
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E3 | 💡 | No automated test asserts the read-only guarantee (SC-002). | The dogfood already checks roadmap mtime before/after. Note that the read-only guarantee is validated by the dogfood mtime check, so it's not unverified — just not a unit test (correct per Principle II). |

### Operational Readiness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E4 | 🤔 | The report accumulates timestamped files under `roadmap-reviews/` with no cleanup. Over many runs this grows. | Low concern (small markdown files, an audit trail is desirable). Optionally note that pruning old reports is the user's call, out of scope here. |

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|--------------------|-----------| 
| X1 | Ambiguous entry match (E2) is also a UX/trust issue (P3). | A wrong match briefs the implementer on the wrong intent. | Matching needs a tie-break-then-ask path. | Add "if match is ambiguous, list candidates and ask" to FR-003 / the command body. |

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendations | 6 (P1,P2,P4,E1,E3) → 5 + E2 = 6 |
| 🤔 Questions | 2 (P3, E4) |

## Recommended Actions

### 🎯 Must-Address
- None.

### 💡 Recommendations
1. **E2/X1**: add an ambiguous-match tie-break ("list candidates + ask") to FR-003 and the
   brief command body. (Highest-value item.)
2. **P4/E3**: note in the spec that drift-detection and read-only are dogfood-validated, not
   unit-tested (Principle II) — prevents a future reader thinking SC-002/SC-003 are untested gaps.
3. **P2**: echo verdict + top findings inline, not only to the report file.

### 🤔 Questions
- **P3**: confirm "stop + suggest" (don't auto-briefing-from-spec) when no entry matches.
- **E4**: confirm report pruning is out of scope (user's call).

---

**Severity Legend**: 🎯 Must-Address · 💡 Recommendation · 🤔 Question

*Generated by `/speckit.critique` — dogfood: a sibling extension reviewing speckit-roadmap's brief spec.*
