# Pre-Implementation Brief Report: 002-roadmap-brief

**Date**: 2026-06-24
**Feature**: [spec.md](../spec.md) (`specs/002-roadmap-brief/`)
**Roadmap**: [.specify/memory/roadmap.md](../../../.specify/memory/roadmap.md) — matched ledger entry **003 — roadmap.brief** (matched by title; roadmap entry numbering ≠ spec-dir numbering, known drift)
**Verdict**: ✅ PROCEED

---

## Summary

Spec `002-roadmap-brief` is on the roadmap (entry 003). The spec faithfully expands the
entry's intent (read-only pre-implementation briefing) and adds the report-template +
severity + verdict standard agreed during design. Its sole dependency — the authoring
command — is already `verified`. No blocking drift. Clear to implement.

---

## Surfaced Context (roadmap entry 003)

- **Entry**: 003 — roadmap.brief (pre-implementation review)  ·  **Status**: planned
- **Description**: Read-only briefing fired by `before_implement` that surfaces what the
  roadmap expects for the spec about to be implemented — outcome, scope, governing
  decisions, dependencies.
- **Outcome (expected)**: Before implement, the implementer sees the entry's recorded
  outcome, in/out scope, governing C-/ADR ids, and dependency specs, with no file mutation.
- **Scope (in)**: the brief command body; entry matching; rendering a briefing report.
- **Scope (out)**: any roadmap/spec mutation; status transitions.
- **Depends on**: 002 (the authoring command + load-config) — **status: verified** ✓
  satisfied (no abandoned/missing dependency).
- **Governed by**: C-03 (strictly read-only). ADR resolution skipped — `adr_present` is
  false (no `docs/adr/`); no ADR pointers to resolve.
- **Addresses**: none (no PRD detected).
- **Related open questions / cross-cutting notes**: Q1 (numbering drift) is relevant and
  is being honored by title-based entry matching. Cross-cutting note "read-only is the
  default for reviews" directly governs this command.

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 💡 | scope-expansion | The spec adds a **shared `review-report-template.md`** (reused by debrief/sync) beyond entry 003's terse "rendering a briefing report". This is an intentional, beneficial elaboration, not contradiction. | Amend entry 003's scope-in to mention the shared report template (minor roadmap update via `/speckit.roadmap.write`). |
| F2 | 🤔 | numbering-drift | Active spec dir is `002-roadmap-brief` but the roadmap entry is `003`. (Roadmap Q1.) | Reconcile entry↔spec-dir numbering during a future `write` amend; not blocking. |

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 1 |
| 🤔 Question | 1 |

## Recommended Actions (proposed only — never auto-applied)

1. **Status**: transition entry 003 → `in-progress` — apply via `/speckit.roadmap.write`.
2. **F1**: fold "shared review-report template" into entry 003's scope — apply via
   `/speckit.roadmap.write`.
3. **F2**: reconcile entry/spec-dir numbering in a later amend.

---

**Severity Legend**: 🎯 Must-Address (blocks) · 💡 Recommendation · 🤔 Question. **This
report is read-only**; apply any roadmap changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.brief`.*
