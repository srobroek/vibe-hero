# Pre-Implementation Brief Report: 002-roadmap-brief

**Date**: 2026-06-24 (formal-flow gate run; supersedes the earlier informal brief-2026-06-24.md)
**Feature**: [spec.md](../spec.md) (`specs/002-roadmap-brief/`)
**Roadmap**: [.specify/memory/roadmap.md](../../../.specify/memory/roadmap.md) — matched ledger entry **003 — roadmap.brief** (matched by title; roadmap entry numbering ≠ spec-dir numbering, known drift Q1)
**Verdict**: ✅ PROCEED

---

## Summary

Fired at the formal `before_implement` gate (between tasks and implement), regenerated
against the current spec (which now includes the critique's FR-003 ambiguous-match
tie-break). Spec 002 faithfully expands roadmap entry 003; its sole dependency (the
authoring command, entry 002) is `verified`. No blocking drift. Clear to implement.

## Surfaced Context (roadmap entry 003)

- **Entry**: 003 — roadmap.brief (pre-implementation review) · **Status**: planned
- **Description**: Read-only briefing fired by `before_implement` surfacing what the
  roadmap expects for the spec about to be implemented.
- **Outcome (expected)**: Before implement, the implementer sees the entry's recorded
  outcome, scope, governing C-/ADR ids, and dependency specs, with no file mutation.
- **Scope (in)**: brief command body; entry matching; rendering a briefing report.
- **Scope (out)**: any roadmap/spec mutation; status transitions.
- **Depends on**: 002 (authoring command + load-config) — **status: verified** ✓.
- **Governed by**: C-03 (read-only). ADR resolution skipped (`adr_present` false).
- **Addresses**: none (no PRD detected).
- **Related**: Open Question Q1 (numbering drift) honored via title matching; cross-cutting
  note "read-only is the default for reviews" governs this command.

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 💡 | scope-expansion | Spec adds the shared `review-report-template.md` (reused by debrief/sync) + the ambiguous-match tie-break (from critique E2), beyond entry 003's terse description. Beneficial elaboration, not contradiction. | Fold "shared review template + tie-break" into entry 003 scope at the next `write` amend. |
| F2 | 🤔 | numbering-drift | spec dir `002-roadmap-brief` ↔ roadmap entry `003`. (Q1.) | Reconcile in a later amend; non-blocking. |

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 1 |
| 🤔 Question | 1 |

## Recommended Actions (proposed only — never auto-applied)

1. **Status**: transition entry 003 → `in-progress` — apply via `/speckit.roadmap.write`.
2. **F1**: fold shared-template + tie-break into entry 003 scope — apply via `/speckit.roadmap.write`.

---

**Severity Legend**: 🎯 Must-Address · 💡 Recommendation · 🤔 Question. **Read-only**; apply
changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.brief` (formal before_implement gate).*
