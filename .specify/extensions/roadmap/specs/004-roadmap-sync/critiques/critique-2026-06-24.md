# Critique Report: Roadmap Sync — Ledger ↔ Reality Reconciliation

**Date**: 2026-06-24
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Verdict**: ✅ PROCEED

---

## Executive Summary

Spec 004 completes the review triad. It is the most logic-heavy of the three reviews (the
status-gating matrix), but still judgment-only and read-only with no new code. The status
pivot is well-specified and is the right design — it directly prevents the false-positive
failure mode that would make a naive sync useless. No must-address items. Recommendations
sharpen the empty-directory definition and the cross-spec dependency-status lookup.

## Product Lens Findings 🎯

### Problem Validation
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P1 | 💡 | sync overlaps conceptually with debrief (both detect drift). Risk of user confusion about which to run. | Clarify in docs: debrief = one spec at its hook; sync = whole ledger, on demand. Already implied by the specs; surface in the README. |

### User Value Assessment
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P2 | 💡 | For a large roadmap, a flat findings list could be noisy. | The report groups by divergence type (already specified) — keep that; consider a summary count line per type at the top. |

## Engineering Lens Findings 🔬

### Architecture Soundness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E1 | 💡 | "empty directory" (phantom trigger) is undefined — a `specs/NNN/` with only a `spec.md` stub vs. real artifacts. | Define empty as "no `spec.md` or only template/placeholder content"; otherwise a just-created spec dir would false-flag as phantom. Tie phantom to "lifecycle status but spec.md absent or still template." |

### Failure Mode Analysis
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E2 | 💡 | dependency-contradiction requires resolving each `depends-on` to another entry's status — a cross-reference within the ledger. | Note that sync resolves depends-on against the ledger's own entries (by number), and flags a depends-on that points to a missing/abandoned entry. |

### Performance & Scalability
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E3 | 🤔 | At very large scale (100s of specs) reading every entry + listing every dir is fine, but reading each spec's contents to judge "empty" could be heavy. | Bound the emptiness check to a cheap signal (spec.md exists and is non-template), not a deep content scan. |

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|--------------------|-----------| 
| X1 | The "empty dir" definition (E1) is also a UX trust issue (false phantoms erode trust). | A false phantom on a fresh spec dir annoys. | Needs a precise, cheap emptiness signal. | Define phantom for lifecycle entries as "spec.md absent or still the unfilled template"; add to FR-006. |

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendations | 4 (P1,P2,E1,E2) |
| 🤔 Questions | 1 (E3) |

## Recommended Actions

### 🎯 Must-Address
- None.

### 💡 Recommendations
1. **E1/X1**: define "empty directory" precisely in FR-006 — a lifecycle-status entry is a
   phantom when its spec dir is absent OR its `spec.md` is missing/still the unfilled template
   (a cheap signal, avoids false-flagging fresh specs). (Highest value.)
2. **E2**: clarify dependency-contradiction resolves `depends-on` against the ledger's own
   entries.
3. **P1/P2**: README should distinguish sync vs debrief; per-type summary counts at report top.

### 🤔 Questions
- **E3**: resolved by the cheap-emptiness-signal in #1 (no deep content scan).

---

**Severity Legend**: 🎯 Must-Address · 💡 Recommendation · 🤔 Question

*Generated by `/speckit.critique` — dogfood: sibling extension reviewing speckit-roadmap's sync spec.*
